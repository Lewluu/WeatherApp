pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building dev-consumer branch '
            }
        }
        stage('Test'){
            steps{
                echo 'Testing dev-consumer branch'
            }
        }
    }
}